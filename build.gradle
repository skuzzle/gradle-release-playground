plugins {
    id 'net.researchgate.release' version '3.0.2'
}

release {
    failOnCommitNeeded = false
    pushReleaseVersionBranch = 'main'
    git {
        requireBranch.set("dev")
    }
}
afterReleaseBuild.dependsOn("generateReadmeAndReleaseNotes")